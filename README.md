Helper utility to load data into Stripe

## Installation
!pip install stripe

Add your Stripe Test API key to the environment, either via a .env, or if you are using google colab, add it to the Secrets.

## Usage
$python airbyte-lms-stripe-testdata\
